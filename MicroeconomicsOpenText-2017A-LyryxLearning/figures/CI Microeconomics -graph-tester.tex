\documentclass[openany,12pt]{book}

\usepackage{mathptmx}
\usepackage[T1]{fontenc}

\usepackage{amssymb}
\usepackage{amsmath}

% Make it look more like the Word docs
\setlength{\parskip}{\baselineskip}
\setlength{\parindent}{0pt}

% Coloring various things
\usepackage[usenames,dvipsnames,table]{xcolor}

% Page layout
\usepackage[top=1in, bottom=1in, left=1in, right=1in]{geometry}

% Rotate one page
\usepackage{pdflscape}

% Figures that don't float
\usepackage{float}

% Used to define new float type for Application Boxes
\usepackage{newfloat}

% For more caption features
\usepackage{caption}

% Better version of cline
\usepackage{hhline}

% Tables that specify columns to stretch automatically
\usepackage{tabularx}

% Tables that can cross page boundaries
\usepackage{longtable}

% Coloring table cells
\usepackage{colortbl}

% Colored tables and other nice enhancements
\usepackage{tabu}

% For multirow cells in tables
\usepackage{multirow}

% Customized part, chapter, section & subsection headers
\usepackage{titlesec}

% Styling sectional headers
\usepackage{sectsty}

% For bold captions
\usepackage[format=hang,font=bf]{caption}

% Adjust margins for part of a page
\usepackage{changepage}

% Adjust line spacing
\usepackage{setspace}

% Multiple columns
\usepackage{multicol}

% Draw text in a box
\usepackage{textpos}

% Used on title page
\usepackage{pagecolor}
\usepackage{afterpage}

\usepackage{graphicx}
\usepackage{epsfig}
\usepackage{epstopdf}

% Draw frames around text
\usepackage[framemethod=tikz]{mdframed}
\mdfsetup{skipabove=\topskip,skipbelow=\topskip}

% because textsubscript doesn't want to work
%\usepackage[version=3]{mhchem}

% Hyperlinks
\usepackage{hyperref}
\hypersetup{
        pdfborder=0 0 0,
        colorlinks=true,
        allcolors=secondcolour, % Default color
        linkcolor=secondcolour, % Chapter/Section links, figures, tables, etc
        urlcolor=secondcolour, % other urls
}

% Glossary
%\usepackage[toc]{glossaries}

% Draw callouts
\usepackage{calc}
\usepackage{tikz}
\usetikzlibrary{calc,intersections,shapes.callouts}
\usetikzlibrary{decorations.text}
\usetikzlibrary{positioning}
\usetikzlibrary{decorations.pathreplacing}
\usetikzlibrary{shadows}
\usetikzlibrary{backgrounds}

% For fancy headers!
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{} % clears all predefined headers and footers
\fancyhead[LO]{\color{grey2}\slshape\nouppercase \leftmark}
\fancyhead[RE]{\color{grey2}\slshape\nouppercase \rightmark}
\fancyfoot[C]{\thepage}
%\fancyfoot[LO,RE]{\color{grey1} Curtis, Irvine -- Microeconomics -- Markets, Methods \& Models}
\renewcommand{\headrulewidth}{0pt} % no rule in header
\setlength{\headheight}{15pt}
\renewcommand{\chaptermark}[1]{\markboth{\chaptername\ \thechapter.\ #1}{}}


% For plotting data
\usepackage{pgfplots}
%\pgfplotsset{compat=1.10}

\usepackage{pgfplotstable}

% Curtis Irvine Style
\usepackage{cistyle}

\begin{document}

\raggedbottom

%\include{graphs-ch1}
%\include{graphs-ch2}
%\include{graphs-ch3}
%\include{graphs-ch4}
%\include{graphs-ch5}
%\include{graphs-ch6}
%\include{graphs-ch7}
%\include{graphs-ch8}
%\include{graphs-ch9}
%\include{graphs-ch10}
%\include{graphs-ch11}
%\include{graphs-ch12}
%\include{graphs-ch13}
%\include{graphs-ch14}
\include{graphs-ch15}

\end{document}
